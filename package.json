{
  "name": "00_demo01",
  "module": "backend.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"cd frontend && bun run dev\" \"bun --watch backend.ts\"",
    "dev:backend": "bun --watch backend.ts",
    "dev:v8": "bun --watch backend.v8.ts",
    "dev:frontend": "cd frontend && bun run dev",
    "build:frontend": "cd frontend && bun run build",
    "build:backend": "bun build ./backend.ts --outdir ./dist",
    "build": "bun run build:frontend && bun run build:backend",
    "start": "bun dist/backend.js",
    "preview:frontend": "cd frontend && bun run preview",
    "db:check": "bun db/health-check.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun db/migrate.ts",
    "db:migrate-json": "bun scripts/migrate-json-to-db.ts",
    "v8:db:setup": "bun scripts/setup-v8-legacy-db.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.6.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "concurrently": "^8.2.2",
    "drizzle-kit": "^0.31.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@elysiajs/openapi": "^1.4.14",
    "@elysiajs/static": "^1.4.7",
    "@neondatabase/serverless": "^1.1.0",
    "drizzle-orm": "^0.45.2",
    "elysia": "^1.4.28",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "zod": "^4.3.6"
  }
}
